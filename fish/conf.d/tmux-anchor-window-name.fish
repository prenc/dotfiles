set -l tmux_anchor_window_name_fish "$HOME/.tmux/plugins/tmux-anchor-window-name/shell/tmux-anchor-window-name.fish"
if test -f "$tmux_anchor_window_name_fish"
    source "$tmux_anchor_window_name_fish"
end
